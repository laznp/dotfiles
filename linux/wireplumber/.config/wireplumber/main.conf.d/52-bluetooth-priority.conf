wireplumber.rules = [
  {
    matches = [
      {
        device.name = "~bluez_*"
      }
    ]
    actions = {
      update-props = {
        priority.driver = 3000
        priority.session = 3000
      }
    }
  }
]

font pango:FuraCode Nerd Font Medium, Iosevka 10
# global variable initialization
set $mod Mod1
set $up k
set $down j
set $left h
set $right l
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $black "#282C34"
set $red "#E06C75"
set $green "#98C379"
set $yellow "#E5C07B"
set $blue "#61AFEF"
set $purple "#C678DD"
set $grey "#ABB2BF"

# media keys
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym $mod+F3 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym $mod+F2 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym $mod+F1 exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym Ctrl+$mod+Left exec playerctl previous
bindsym Ctrl+$mod+Right exec playerctl next
bindsym Ctrl+$mod+space exec playerctl play-pause
floating_modifier $mod
bindsym $mod+Return exec alacritty
bindsym $mod+Shift+q kill
bindsym $mod+space exec --no-startup-id "rofi -no-lazy-grab -no-history -no-sort -show drun -modi ssh,drun,calc -theme /home/laznp/.config/rofi/config"
# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
# split in horizontal orientation
bindsym $mod+minus split v
# split in vertical orientation
bindsym $mod+Shift+backslash split h
# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split
# toggle tiling / floating
bindsym $mod+Shift+space floating toggle
# focus the parent container
#bindsym $mod+a focus parent
# focus the child container
#bindsym $mod+d focus child
# move the currently focused window to the scratchpad
bindsym $mod+Shift+plus move scratchpad
# Show the next scratchpad window or hide the focused scratchpad window.
bindsym $mod+plus scratchpad show
# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "bash $HOME/.config/rofi/powermenu.sh"
# resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        bindsym $left       resize shrink width 5 px or 5 ppt
        bindsym $down       resize grow height 5 px or 5 ppt
        bindsym $up         resize shrink height 5 px or 5 ppt
        bindsym $right      resize grow width 5 px or 5 ppt
        bindsym Left        resize shrink width 5 px or 5 ppt
        bindsym Down        resize grow height 5 px or 5 ppt
        bindsym Up          resize shrink height 5 px or 5 ppt
        bindsym Right       resize grow width 5 px or 5 ppt
        # back to normal
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bar {
    status_command i3blocks
}

# Caps Lock Indicator
#set $mode_caps 
#bindsym Caps_Lock mode "$mode_caps"
#bindsym Shift+Caps_Lock mode "$mode_caps"
#bindsym Ctrl+Caps_Lock mode "$mode_caps"
#bindsym $alt+Caps_Lock mode "$mode_caps"
#mode "$mode_caps" {
#        bindsym Caps_Lock mode "default"
#        bindsym Shift+Caps_Lock mode "default"
#        bindsym Ctrl+Caps_Lock mode "default"
#        bindsym $alt+Caps_Lock mode "default"
#}
# Cycling Workspace
bindsym Ctrl+Right workspace next
bindsym Ctrl+Left workspace prev
# Open Terminal File Manager
bindsym $mod+Shift+F exec alacritty -t "ranger" -e "ranger"
bindsym $mod+E exec thunar
# Screenshot
bindsym Print --release exec "import png:- | xclip -selection clipboard -t image/png -filter && notify-send Screenshot 'Saved to clipboard.'"
# Lock Screen
bindsym $mod+shift+x exec betterlockscreen -l dim
# Color Scheme Configuration
# class						border		backgr.		text		indicator		child_border
client.focused				$blue 		$blue 		$black		$blue 			#5C6570
client.focused_inactive		$black 		$black		$grey		$black			$black
client.unfocused			$black 		$black		$grey 		$black			$black
client.urgent				$red 		$red 		$black		$red 			$red
client.placeholder			$purple 	$purple 	$black		$purple 		$purple
# i3 Gaps
for_window [class="^.*"] border pixel 1
for_window [class="^.*"] split toggle
gaps inner 10
gaps outer 0
# Assign Windows
assign [instance="^(?i)google-chrome" class="^(?i)google-chrome"] $ws1
assign [instance="^(?i)brave-browser" class="^(?i)brave-browser"] $ws1
assign [class="Alacritty"] $ws2
assign [class="Lens"] $ws3
assign [class="Insomnia"] $ws3
assign [class="VirtualBox Manager"] $ws3
assign [class="TelegramDesktop"] $ws5
for_window [class="Slack"] move to workspace $ws4
for_window [class="discord"] move to workspace $ws4
for_window [class="Spotify"] move to workspace $ws6
for_window [title="GIMP Startup"] move workspace $ws7
for_window [class="Gimp"] move workspace $ws7
assign [class="vlc"] $ws8
assign [class="Thunar"] $ws9
# Move workspace when monitor detected
workspace $ws1 output HDMI-1
workspace $ws2 output HDMI-1
workspace $ws3 output HDMI-1
workspace $ws4 output HDMI-1
workspace $ws5 output HDMI-1
workspace $ws6 output eDP-1
workspace $ws7 output HDMI-1
workspace $ws8 output HDMI-1
workspace $ws9 output HDMI-1
workspace $ws10 output HDMI-1
# Fibonacci Layout
# exec --no-startup-id i3-fibbonaci && notify-send "i3" "Autotilling Started."
# Polybar
# exec_always --no-startup-id $HOME/.config/polybar/launch.sh
# Libinput gesture
exec_always --no-startup-id libinput-gestures-setup start
# TLP
exec_always --no-startup-id sudo tlp start
# Xrandr
exec xrandr --auto --output HDMI-1 --mode 1920x1080 --rate 75 --left-of eDP-1
# Dunst
exec --no-startup-id dunst -config ~/.config/dunst/dunstrc
# Feh Background
exec_always --no-startup-id feh --bg-scale $HOME/.wallpaper.png
# Picom
exec_always --no-startup-id picom --config $HOME/.config/picom/picom.conf
# Alacritty
#exec alacritty
# Spotify
exec spotify
# Google Chrome
exec google-chrome-stable
# Brave
exec brave


